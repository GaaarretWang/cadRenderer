# cadRenderer General Rules

本文件是仓库内的通用规则总入口。调试、构建、compare 的专项规则见
[tools/debug/rules.md](tools/debug/rules.md)。
渲染管线专项知识库见 [RENDERER_KNOWLEDGE.md](RENDERER_KNOWLEDGE.md)。

## 第一屏硬约束

1. 重大改动先整理方案；需求或旧逻辑不清时，先停下来确认。
2. 所有重构必须拆成小步；每一步完成后项目都要能编译、运行并给出可验证结果。
3. 小步不等于碎步。要在“稳定可定位”和“推进速度”之间取平衡，默认以一个可独立定位的问题域作为一个 commit，不要因为过度保守拖慢主线推进。
4. 高风险改动先并行接入新逻辑，验证通过后在后续独立步骤删除旧逻辑。
5. 一旦确认旧链已经被稳定替代，就应及时清理，不要长期双轨并存。
6. 提交前必须清理已经确认无用的代码、分支逻辑和临时残留。
7. 发现旧代码存在 bug 或语义自相矛盾时，不要基于错误理解继续开发。
8. 未经用户明确允许，不要修改 `RenderingServer` 对外接口语义，也不要提交代码。
9. 官方运行脚本必须保持可用：Linux 侧 `run.sh`，Windows 侧 `build_and_run.bat`。

## 通用开发规则

### 方案与拆解

- 大改动先拆任务，再动代码。
- 如果一个动作会同时牵涉多个子系统，先把基础设施和前置依赖准备好，再改上层调用。
- 优先做高内聚、低耦合的改动，避免继续叠加无意义判断。

### 重构与清理

- 风险大的替换采用增量并行方式，不要直接整段硬拔旧实现。
- 新逻辑效果确认正确后，下一独立步骤要主动删掉旧代码。
- 如果整理代码后效果与之前不一致，先回到最近一次稳定状态再定位差异。

### 接口与兼容性

- `RenderingServer.h` / `RenderingServer.cpp` 中对外接口默认受保护。
- 必须持续保持与现有 AR/MR 接入方式的兼容性。
- 需要修改接口时，先明确影响面，再征得用户确认。

### 目录与上下文

- 开始改动前先看 `DIRECTORY_STRUCTURE.md`，按目录结构定位代码。
- 优先缩小上下文，只读与当前任务直接相关的文件。

### 效果与审查

- 涉及渲染效果的改动，要给出清晰的验证方式和观察点。
- 用户要求审查效果时，先描述方案和预期，再推进后续改动。
- 如果改动不改变画面效果或外部行为，默认先自动化测试；测试通过后直接进入下一个 commit，不等待用户逐步确认。

## 测试与运行原则

- 渲染测试优先让程序自然退出，不依赖脚本内部硬编码时间限制。
- 运行、build、compare 的具体操作规则和节奏，统一以
  [tools/debug/rules.md](tools/debug/rules.md) 为准。
- 测试脚本和运行脚本改动后，必须验证它们自身仍可用。
- 我自己执行调试、构建、compare、git、删除文件、清理进程等命令时，
  一律先改 `tools/debug/agent_debug.bat`，再从这个万能 bat 启动；
  不再默认直接执行零散命令。
- 统一调用格式固定为：在仓库根目录下执行
  `& .\tools\debug\agent_debug.bat`；
  后续我不再切换成别的调用写法。
- 默认每个 commit 只做必要的自动化验证，不做无差别重复重测。
- 只有在改动渲染效果、切主路径、删除旧链、或出现不稳定迹象时，才升级到更重的验证和人工画面确认。

## 安全规则

- 未经明确要求，不要执行破坏性操作。
- 未经明确要求，不要提交代码或改写用户现有分支历史。
- 重大修改后必须验证渲染输出质量，而不是只看编译是否通过。

